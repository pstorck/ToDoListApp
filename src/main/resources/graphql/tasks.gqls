type Task {
    id: ID!
    description: String!
    completed: Boolean!
}

type Query {
    tasks: [Task]!
    task(id: Int): Task
}

type Mutation {
    toggleComplete(id: ID!): Task
    createTask(description: String!): Task
    deleteTask(id: ID!): ID!
}